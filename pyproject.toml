[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "autotest-mcp"
version = "1.0.0"
description = "Industrial-grade unit test and defensive refactoring Model Context Protocol (MCP) server for Cursor and Claude Desktop."
readme = "README.md"
authors = [{ name = "AutoTest AI" }]
license = { text = "MIT" }
dependencies = []
requires-python = ">=3.9"

[project.scripts]
autotest-mcp = "mcp_server:main"
